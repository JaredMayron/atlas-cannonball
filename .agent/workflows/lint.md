---
description: Run ruff check and format automatically
---

// turbo
1. Run ruff checks and formatting on the source and tests.
   `ruff check GCP/src GCP/tests --fix && ruff format GCP/src GCP/tests`
